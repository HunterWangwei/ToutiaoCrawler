## v1.7.1：自动更新流程重构

本版本重写自动更新安装流程，取消覆盖正在运行的旧版 EXE。

### 新更新流程

1. 新版下载到当前软件目录，临时名称为 `ToutiaoCrawler-版本号.exe.part`。
2. 下载完成后验证 Release 提供的 SHA256。
3. 校验通过后改名为 `ToutiaoCrawler-版本号.exe`。
4. 直接启动带版本号的新 EXE。
5. 新版启动命令成功后关闭旧版本。

### 稳定性改进

- 不再覆盖或删除正在使用的旧版 EXE。
- 不再使用 PowerShell 等待旧进程、替换文件或延迟重启。
- 旧版保留作为回退文件，确认新版正常后可手动删除。
- 新旧不同文件名的 EXE 已通过同时运行验证。
- 保留下载百分比、备用更新代理和 SHA256 强制校验。

### 使用提醒

- 当前 EXE 所在目录必须具有写入权限。
- `python312.dll` 是单文件 EXE 内置运行库，启动时会释放到 Windows 临时目录。

### 下载

请下载本 Release 中的 `ToutiaoCrawler.exe`。`ToutiaoCrawler.exe.sha256` 可用于校验文件完整性。
