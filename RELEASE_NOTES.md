## v1.7.2：测试版自动更新识别修复

本版本修复带 `-test` 后缀的测试版无法识别同数字正式版的问题。

### 修复内容

- `1.7.1-test` 现在会被正确判断为低于 `1.7.1` 正式版。
- 测试版检查更新时可以发现并下载同数字正式版本。
- 预发布版本按以下顺序比较：

```text
alpha < beta < test < rc < 正式版
```

- 数字版本仍然优先，例如 `1.7.2-test` 高于 `1.7.1`。
- 并排下载、SHA256 校验、更新进度和备用更新代理逻辑保持不变。

### 重要说明

已经生成的旧版 `1.7.1-test` 本身仍包含旧比较逻辑，无法通过自身发现 `1.7.1`。需要手动安装一次 `v1.7.2`，之后的新测试版即可正常更新到正式版。

### 下载

请下载本 Release 中的 `ToutiaoCrawler.exe`。`ToutiaoCrawler.exe.sha256` 可用于校验文件完整性。
