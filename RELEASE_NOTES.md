## v1.7.3：更新代理优先级与自动重试

本版本修复“手动填写S5代理可以更新，但自动使用内置代理仍可能失败”的问题。

### 原因

部分网络可以直连 `api.github.com` 检查版本，却无法稳定直连 `github.com` 和 `release-assets.githubusercontent.com` 下载附件。旧逻辑先使用直连，导致检查版本成功后在下载阶段发生超时或 `RemoteDisconnected`。

### 新通道顺序

用户填写S5代理时：

```text
用户代理 → 内置代理 → 直连
```

用户未填写S5代理时：

```text
内置代理 → 直连
```

### 稳定性改进

- 版本检查、SHA256下载和EXE下载采用相同的代理优先顺序。
- 每个通道发生连接中断、超时等错误时自动重试3次。
- 当前通道重试失败后才切换下一通道。
- 所有通道均失败时，错误窗口列出各通道每次尝试的具体原因。
- 并排下载、SHA256强制校验和带版本号启动流程保持不变。

### 下载

请下载本 Release 中的 `ToutiaoCrawler.exe`。`ToutiaoCrawler.exe.sha256` 可用于校验文件完整性。
