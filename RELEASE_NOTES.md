## v1.7.0：个人主页采集模式与备用更新通道

### 个人主页采集模式

- 新增“内容 + 图片 + 评论”和“仅内容 + 图片”两种模式。
- 选择“仅内容 + 图片”后不请求评论接口，TXT 不生成空评论区。
- 最低评论数仍可作为作品筛选条件，因为它读取作品详情中的评论总数。
- 采集模式自动保存到配置文件。

### 自动更新备用通道

- 用户填写的更新代理或本机直连 GitHub 失败时，自动尝试构建时内置的备用更新代理。
- 界面会提示正在切换备用更新通道。
- 备用代理凭据通过 GitHub Actions Secret 注入，不写入公开源码和文档。
- 下载完成后仍必须通过 SHA256 校验才会安装。

### 下载

请下载本 Release 中的 `ToutiaoCrawler.exe`。`ToutiaoCrawler.exe.sha256` 可用于校验文件完整性。
