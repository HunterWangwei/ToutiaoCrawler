# 更新记录

## 1.7.0 — 2026-08-12

### 个人主页采集模式

- 新增“内容 + 图片 + 评论”和“仅内容 + 图片”两种采集模式。
- 仅内容与图片模式不会请求评论接口，TXT 不生成评论标题或空评论区。
- 最低评论数仍可正常筛选作品，采集模式会自动保存到配置文件。
- 个人主页命令行工具同步增加 `--with-comments` 和 `--no-comments` 参数。

### 自动更新备用代理

- 用户填写的代理或本机直连 GitHub 失败时，自动尝试内置备用更新通道。
- 更新状态栏会提示正在切换备用代理。
- 备用代理通过 GitHub Actions Secret 注入，不在公开源码和文档中保存凭据。
- 备用代理仅用于 GitHub 更新检查和下载，不影响内容采集代理设置。
- Release 工作流改为读取 `RELEASE_NOTES.md`，避免更新说明缺失。

## 1.6.1 — 2026-08-11

### 自动更新稳定性与进度显示

- 更新下载过程中新增百分比进度条。
- 同时显示已下载大小和文件总大小；服务器未返回总大小时显示已下载容量。
- 修复单文件 EXE 自动更新时只等待一个进程的问题。
- 安装器现在会等待旧版 PyInstaller 父、子进程全部退出后再替换文件。
- 新版文件替换完成后延迟数秒再启动，降低安全软件扫描期间出现 `python312.dll` 加载失败的概率。
- 保留下载完成后的 SHA256 校验，校验失败时不会安装。

## 1.6.0 — 2026-08-11

### 多主页批量采集

- 个人主页采集新增“导入主页 TXT”，支持每行填写一个个人主页地址。
- 导入时自动识别有效主页、保持原始顺序并去除重复地址。
- 保留手动输入单个主页的使用方式。
- 多个主页依次处理，每个主页内部继续使用设置的并发线程采集作品。
- 每个主页仍按作者名建立独立目录，正文、图片、日志和 `posts.json` 不会互相混合。
- 状态区域显示当前主页序号、主页总数和当前采集作者。
- 单个主页读取失败时自动继续下一个主页，结束汇总显示主页读取失败数量。
- 自定义时间段的开始日期首次默认使用本机昨天，结束日期首次默认使用本机当天。
- 已保存的日期配置继续优先使用，不会在每次启动时被默认日期覆盖。

## 1.5.1 — 2026-08-11

### 个人主页文本清理

- 自动删除个人主页微头条正文结尾的 `【lm】`、`【gmj】` 标识。
- 同时兼容半角方括号和大小写形式，只清理正文末尾，不误删正文中间的正常内容。
- 高赞评论仍按点赞数排序，但保存到 TXT 时不再附加“（赞 N）”。

## 1.5.0 — 2026-08-11

### 个人主页作品筛选

- 新增“最低评论数”，作品总评论数低于设置值时直接跳过，`0` 表示不限制。
- 新增发布时间筛选，可选择“不限时间”“仅今天”或“自定义时间段”。
- 自定义时间段使用 `YYYY-MM-DD`，并包含开始日和结束日。
- 时间筛选启用时，发布时间未知的作品会跳过，避免混入不符合条件的内容。
- 评论数和时间过滤均发生在评论、图片采集之前，减少无效请求。
- 每篇作品状态显示具体原因，任务汇总分别统计评论数不足和时间过滤数量。
- 新设置自动保存到 `config.json`，下次启动继续使用。

## 1.4.1 — 2026-08-11

### 个人主页作者目录与状态增强

- 个人主页采集开始后自动识别并显示当前采集作者。
- 输出结构调整为“保存目录/主页作者名/内容、图片”。
- `posts.json` 和个人主页日志也保存到对应作者目录。
- 作者名自动清理 Windows 文件名不支持的字符。
- 作者无法识别时使用主页 Token 生成不冲突的备用目录名。
- 微头条链接采集和个人主页采集的违禁词状态均显示具体命中词。
- 多个违禁词同时命中时显示为“含违禁词：政治、中央”。

## 1.4.0 — 2026-08-11

### 个人主页采集合并

- UI 改为“微头条链接采集”和“个人主页采集”两个独立标签页。
- 原有功能完整迁移到“微头条链接采集”分类。
- 合并个人主页分页发现微头条的协议采集功能。
- 个人主页采集支持独立保存目录、S5 代理和可选 Cookie 文件。
- 支持限制主页抓取页数，`0` 表示抓到末页。
- 支持个人主页作品 `1–5` 线程并发处理。
- 支持独立评论数量和违禁词过滤。
- 主页中的每条作品独立显示正文、评论、图片、完成、失败或违禁词状态。
- 生成 `posts.json` 和独立的个人主页采集日志。
- 两个功能页分别管理任务状态和停止控制，避免新旧功能相互冲突。

## 1.3.1 — 2026-08-11

### 违禁词配置持久化

- 违禁词输入内容变化后自动保存到 `config.json`。
- 软件重新启动时自动恢复上次填写的违禁词。
- 优先将配置保存在 EXE 同目录，便于便携使用。
- EXE 目录不可写时自动回退到 `%APPDATA%\ToutiaoCrawler\config.json`。
- 使用临时文件替换方式写入配置，降低写入中断造成配置损坏的风险。

## 1.3.0 — 2026-08-11

### 违禁词过滤

- 新增“违禁词”输入框。
- 默认违禁词为 `政治|中央|证券|央行`。
- 支持使用半角 `|` 或全角 `｜` 分隔多个词。
- 同时检查文章标题和正文。
- 命中任意违禁词时，状态显示“含违禁词”。
- 命中后直接跳过评论抓取、图片下载和 TXT 保存。
- 日志记录该文章命中的具体违禁词。
- 任务结束汇总中新增违禁词过滤数量。

## 1.2.0 — 2026-08-09

### 多线程采集

- 新增并发线程设置，支持 `1–5` 线程。
- 默认使用 `3` 个线程并行采集。
- 每条链接使用独立采集器，避免线程间共享网络连接状态。
- 正文、评论、图片及失败状态仍按链接分别显示。
- 日志写入增加线程锁，避免并发写入造成内容混乱。
- 点击停止后，尚未开始的任务会标记为“已停止”。

## 1.1.0 — 2026-08-09

### 版本与自动更新

- 窗口标题和软件底部新增当前版本号显示。
- 软件启动后自动检查 GitHub Releases。
- 新增“检查更新”按钮，可随时手动检查。
- 发现新版后支持自动下载 Windows EXE。
- 下载完成后验证 SHA256，校验失败时拒绝安装。
- 校验通过后自动替换当前 EXE 并重启软件。
- 源码运行模式下自动打开 GitHub Release 页面。
- 新增 GitHub Actions，推送 `v*` 标签时自动编译并发布 EXE。

## 1.0.0 — 2026-08-09

### 采集功能

- 支持批量采集今日头条微头条正文。
- 支持下载正文中的全部图片。
- 支持抓取多页评论并保留点赞最高的 `5–10` 条。
- 评论点赞相同时，优先保留清洗后字数更多的评论。
- 点赞数和字数相同时，按回复数排序。

### 评论清洗

- 删除 `[飞吻]`、`[捂脸]`、`[鼓掌]`、`[招财进宝]` 等文本表情。
- 删除 👏、👍、😂、❤️ 等 Unicode 表情和图形符号。
- 保留删除表情后的正常评论文字。
- 自动丢弃清洗后没有有效文字的评论。
- 排除包含 `@豆包` 的自动问答类评论。

### 图形界面

- 新增 Windows 桌面 UI。
- 支持把 TXT 文件拖入窗口导入链接。
- 支持通过按钮选择 TXT 文件。
- 自动提取链接并去重。
- 每条链接独立显示采集正文、采集评论、下载图片、完成或失败状态。
- 状态中实时显示当前阶段等待秒数。
- 支持停止剩余任务。
- 支持选择保存目录和评论数量。

### 代理与网络稳定性

- 支持无认证和带认证的 SOCKS5/SOCKS5H 代理。
- 禁止读取失效的系统环境代理，避免其他电脑出现请求卡死。
- 增加连接超时、读取超时和请求硬超时。
- 网络失败自动重试两次，失败后继续处理下一条链接。
- 新增 `采集日志.txt`，记录每条链接的处理结果及详细错误。

### 输出格式

- 正文和评论合并保存为 TXT。
- TXT 中不保存标题、摘要、作者、发布时间、原文链接和文章点赞数。
- 内容和图片分别保存到 `内容`、`图片` 文件夹。
- TXT 文件名与对应图片子文件夹名保持一致。
- TXT 使用 UTF-8 BOM 编码，兼容 Windows 记事本。

### 发布

- 提供无需安装 Python 的单文件 Windows EXE。
- 内置拖拽、网络请求、证书和 SOCKS5 运行依赖。
